# GPAL MK-VII

這是一個 HTML 單頁 app，兩個檔案：
- gpal_optimized.html（完整版）
- gpal_family.html（家庭版）

每次修改後：
1. 同步更新兩版版本號（4 處：title、brand span、meta version、CURRENT_VERSION 常數）
2. git commit 並 push 到 main branch
3. GitHub Pages 會自動重建